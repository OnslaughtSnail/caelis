name: quality

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  go-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: "1.25.1"
          cache: true

      - name: Verify formatting
        run: make fmt-check

      - name: Lint
        uses: golangci/golangci-lint-action@v9.2.0
        with:
          version: v2.11.4
          args: ./...
          install-mode: goinstall

      - name: Vet
        env:
          GOFLAGS: -p=1
          GOMAXPROCS: 2
        run: make vet

      - name: Build
        env:
          GOFLAGS: -p=1
          GOMAXPROCS: 2
        run: make build

      - name: Test
        env:
          GOFLAGS: -p=1
        run: |
          set -o pipefail
          go test -json -timeout=5m ./... | tee /tmp/go-test.json
